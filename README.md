# tasks and algorithms from 
"The C programming language by Kernighan Ritchie"

# Build (will prefer main if present)
make

# Run with an arguments (example)
./main "ould"
hello world
we could test this
Ctrl+D  
# program does not exit until you send EOF (Ctrl+D).

# Clean artifacts
make clean